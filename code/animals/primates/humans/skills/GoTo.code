import Human;

human = super Human;

void GoTo(locationCode)
{
    location = locationFactory(locationCode);
    locationStrategy = locationStrategyFactory(locationCode);

    locationStrategy.exec(location);
}